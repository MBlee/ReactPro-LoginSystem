import React from 'react'
import {connect} from 'react-redux'

function LoginForm() {
	
	return (
		<div className='LoginForm'> 
			LoginForm
		</div>
	)
}
export default LoginForm
// export default connect(res=>res)(LoginForm)